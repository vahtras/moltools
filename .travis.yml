language: python
python:
  - 2.7
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
# install: PLEASE CHANGE ME
# command to run tests, e.g. python setup.py test
install:
  pip install coveralls

script: 
  nosetests --with-coverage --cover-package=molecules,dic,read_dal,template,use_calculator,use_generator,ut,xms,graph

after_success:
  coveralls
